# JINSUL MAP v3.5.2 API 수정·검증 결과

검증일: 2026-09-07. 기준 파일: JINSUL_MAP_v3.5.1.html.

**코드 수정 및 로컬 회귀 검증은 완료했지만, 운영 API 연결 성공은 미검증입니다.** 실행 환경의 외부 네트워크 권한 제한으로 R-ONE 시험 요청이 HTTP 응답 전에 `EACCES`로 차단되었습니다. 이 오류는 사용자 API의 승인 여부나 Cloudflare 운영 장애를 입증하지 않습니다. 제한을 우회하거나 운영 서버 설정을 변경하지 않았습니다.

## 확인한 원인과 수정

| 항목 | v3.5.1 문제 | v3.5.2 처리 |
|---|---|---|
| 병원명 검색 → 건축물대장 | 좌표 주소 응답의 `b_code`에 의존하여 법정동 코드가 누락될 수 있음 | `coord2Address`로 지번, `coord2RegionCode`의 법정동(B) 결과로 법정동 코드를 확보 |
| 상업용 실거래 | SGIS 통계 시군구 코드를 `LAWD_CD`에 전달 | 법정동 10자리의 앞 5자리만 사용. 코드 미확정 시 조회 생략 사유 표시 |
| 부동산 API 연쇄 미조회 | SGIS 처리가 끝나야 부동산 요청이 시작되고, SGIS 실패 + 법정동 누락 시 R-ONE도 생략 | 건축물·실거래·R-ONE을 SGIS와 독립 실행 |
| 실거래 실패 표시 | 월별 호출이 전부 실패해도 0건 데이터 생성 | 전체 실패 시 조회 불가, 일부 실패한 월은 그래프 값 `null` |
| 공공 API 응답 해석 | 알 수 없는 JSON/XML 및 상가 API `header.code` 오류가 빈 성공 결과가 될 수 있음 | 응답 구조 및 오류 코드 검사 강화 |
| R-ONE | 최상위 RESULT 오류·프록시 오류를 빈 통계로 처리할 수 있음 | 오류 구분, 빈 통계 캐시 제거, 재조회 허용 |
| HIRA 상세 | 세 요청 모두 실패해도 빈 성공 객체가 캐시됨 | 전체 실패는 오류, 실패·부분 응답은 캐시 제거 후 재조회 허용 |
| 서울 공식상권 | 점포 호출 실패를 점포 0곳으로 표시 | 조회 불가로 표시. 양쪽 호출이 실패하면 API 연결 성공으로 표시하지 않음 |
| 오류 안내 | 승인 여부·네트워크 문제를 뭉뚱그려 안내 | HTTP 상태·시간 초과·응답 형식·네트워크/CORS/HTTPS 가능성을 구분. 응답 원문·인증키는 안내에 표시하지 않음 |

브라우저의 일반적인 fetch 실패만으로 CORS, TLS, DNS, 방화벽 중 어느 것이 원인인지 확정할 수는 없습니다. 프록시가 필요하다는 판단 역시 실제 배포 환경의 응답 확인 후 내려야 합니다.

## API 자료 대조

첨부 파일의 서울 실시간 인구·서울 상권·HIRA 병원정보·HIRA 상세정보·건축물대장·상업용 실거래 키는 HTML 값과 일치합니다. SGIS ID와 Secret도 일치합니다. 키를 교체하거나 다시 발급할 근거는 확인되지 않았습니다.

R-ONE은 지정한 `https://jinsul-rone-proxy.yms0127.workers.dev/rone`을 유지합니다. R-ONE 인증키를 HTML에 추가하지 않았습니다. 기존 SDK·라이브러리와 인증 설정도 유지했습니다.

전국 버스정류장 API는 v3.5.1에 호출·표시 기능이 없습니다. 이 항목은 연결 실패가 아닌 미구현이며, 이번 연결 오류 수정 범위에서 새 기능을 추가하지 않았습니다. 전국 반경 상가 API는 기존 설정을 유지했으며, 이번 첨부 목록에는 별도 소상공인 API 파일이 없습니다.

## 왕십리본정형외과 사례

[심평원 병원정보](https://www.hira.or.kr/ra/hosp/hospInfoAjax.do?ykiho=JDQ4MTg4MSM1MSMkMiMkNCMkMDAkNTgxOTYxIzExIyQxIyQ3IyQwMyQyNjE4MzIjNTEjJDEjJDIjJDgz)의 주소는 서울특별시 성동구 왕십리로 320, W에비뉴타워 3층(도선동)입니다. 해당 페이지 길찾기 링크의 좌표는 위도 37.5623492, 경도 127.0343485입니다.

사용자가 반경을 지정하지 않아 **1km를 검증 조건으로 사용**했습니다. 내장 지점·상권 목록을 실제 함수에 넣은 결과:

- 서울 실시간 인구 지정지점: 왕십리역 약 401m
- 공식 상권 중심점: 행당시장상점가 약 158m, 왕십리도선동상점가 약 209m, 왕십리역(왕십리) 약 299m 등
- 100m 반경에서는 실시간 인구 지정지점 없음으로 처리

이는 지리 선택 로직의 검증이며, 해당 API의 실시간 데이터 수신 성공을 뜻하지 않습니다. 1km에서 서울 항목이 미연결이라면 비서울 지역 제외 조건 때문은 아니므로 호출 응답을 확인해야 합니다. 표시되는 인구·상권 값은 해당 지정지점 또는 상권 통계이며 병원 중심 반경 합산값이 아닙니다.

## 검증 범위

- 자동 검사 **34/34 통과**: JavaScript 구문, 기존 DOM ID·함수·외부 SDK·인증 설정 보존, 정상/빈/오류 JSON, 요청 실패 분류, 법정동 구분, 실거래 전체·부분 실패, 독립 요청, 오래된 응답 차단, 캐시 재시도, 서울 지리 조건.
- v3.5.1의 정적 ID 212개 보존, 중복 ID 없음.
- 원본 v3.5.1 파일 수정 없음. 신규 파일 JINSUL_MAP_v3.5.2.html 제공.
- 테스트는 Node VM에서 실제 함수와 모의 응답을 사용했습니다. 실제 브라우저 조작, 실제 XML DOM 파싱 실행, 운영 API 수신, CORS 및 배포 환경 전체 흐름은 검증하지 못했습니다.

재현: 저장소 루트에서 `node work/test_api_v352.cjs` 실행. 상세 결과는 `work/api-test-results.json`, 지리 결과는 `work/wangsimni-test-results.json`에 저장됩니다.

외부 API 호출이 허용된 환경에서 `node work/probe_apis.cjs`로 추가 진단할 수 있습니다. 해당 스크립트는 HTML의 기존 설정을 읽고 키·요청 URL·응답 본문을 출력하지 않습니다. Node 통신 점검이므로 브라우저의 CORS 허용 성공을 보장하지 않으며, HIRA 상세의 실제 조회는 유효한 요양기관 식별자로 추가 확인해야 합니다. 이번 실행에서는 스크립트 구문만 검사했습니다.

## 공식 사양 참고

- [국토부 상업업무용 실거래 명세](https://www.data.go.kr/data/15126463/openapi.do): 법정동 코드 앞 5자리와 계약년월 사용, XML 응답.
- [카카오 Geocoder SDK](https://apis.map.kakao.com/web/documentation/#services_Geocoder): 주소·행정구역 코드 조회 메서드.
- [카카오 로컬 응답 구조](https://developers.kakao.com/docs/ko/local/dev-guide): 좌표→주소와 좌표→행정구역 코드의 분리.
- [서울 API 이용 안내](https://data.seoul.go.kr/together/guide/useGuide.do): 서비스명·행 범위 요청 방식.

GitHub 업로드 및 Cloudflare 배포는 수행하지 않았습니다.

