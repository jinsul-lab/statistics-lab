# JINSUL MAP 작업 규칙

이 문서는 이 저장소의 JINSUL MAP 작업에 적용한다. 다른 통계·차트 프로젝트에 JINSUL MAP 전용 규칙을 일괄 적용하지 않는다.

## 기본 원칙
- 요청 해결에 필요한 연관 부분까지 수정하되, 불필요한 변경은 하지 않는다.
- 기존 정상 기능과 UI 배치를 보존한다.
- 정상 데이터 처리 로직과 통계 계산식은 보존한다. 명시적 요청 또는 확인된 오류를 수정할 때는 변경 근거와 영향을 기록한다.
- 사용자의 미커밋 변경을 임의로 덮어쓰거나 되돌리지 않는다.
- 수정 전후 관련 동작을 검증한다. 무관한 리팩터링이나 디자인 변경은 함께 진행하지 않는다.

## 기준 파일과 산출물
- 현재 기준 파일·버전·배포 상태는 `HANDOFF.md`에서 확인한다.
- 같은 버전의 사본이 여러 곳에 있으면 기준 파일을 먼저 확정하고, 배포에 필요한 사본만 동기화한다.
- 최종 앱은 단일 HTML로 제공한다. 검증 스크립트와 문서는 별도 파일로 관리할 수 있다.
- 단일 HTML이 외부 SDK와 API에 의존한다면 오프라인에서도 전부 작동한다고 설명하지 않는다.
- 기준 파일이나 배포 경로가 바뀌면 `HANDOFF.md`도 갱신한다.

## 버전
- `MAJOR.MINOR.PATCH` 형식을 사용한다. 예: `3.5.2 → 3.5.3`.
- 최종 HTML 파일명과 화면의 버전을 일치시킨다.
- 기능 수정이 없는 문서 변경에는 HTML 버전을 올리지 않는다.
- 버전별 변경 이력은 `CHANGELOG.md`에 기록한다.

## 보존 대상
다음 항목은 명시적 요청이나 확인된 오류가 없으면 변경하지 않는다. 오류 수정이 필요하면 관련 동작을 검증한다.
- 환자 데이터 업로드 기능
- 통계 계산식과 데이터 처리 기준
- 마커 색상 규칙
- 로드뷰와 지도 도구
- 기존 필터 명칭과 동작
- 후보지 비교 및 상권 분석

## API와 데이터
- API 조회 실패·자료 없음·실제 0건을 구분한다.
- 일부 기간이나 항목의 조회가 실패하면 전체가 정상 조회된 것처럼 표시하지 않는다.
- 데이터의 공간 범위, 기준 시점, 출처를 유지한다. 조사권역·행정구역 통계를 후보지 반경 합산값으로 표시하지 않는다.
- 인증키·비밀번호를 문서, 로그, 테스트 결과, 커밋 메시지에 기록하지 않는다.
- 기존 내장 인증정보를 새 파일·문서에 불필요하게 복제하지 않는다.
- R-ONE 인증키는 Worker Secret으로 유지한다.

## 검증과 배포
- 실제 API 검증과 모의 응답 테스트를 구분한다.
- 검증하지 못한 기능을 정상 작동한다고 보고하지 않는다.
- HTML 변경이 프록시를 덮어쓰지 않도록 배포를 분리한다. 프록시 전용 자동 배포를 구성할 경우 대상 코드와 트리거 범위를 확인한다.
- GitHub 업로드 성공, 사이트 배포 완료, 실제 API 동작 성공을 각각 구분한다.
- 원격 변경을 보존하며, 강제 push는 명시적 요청 없이 하지 않는다.

## 작업 완료 시 기록
1. 변경사항과 이유
2. 수정한 파일
3. 테스트 결과와 미검증 항목
4. 최종 버전
5. 커밋·업로드·배포 여부

작업 규칙은 이 문서, 현재 상태는 `HANDOFF.md`, 버전별 이력은 `CHANGELOG.md`, 상세 검증은 별도 보고서에 기록한다.
