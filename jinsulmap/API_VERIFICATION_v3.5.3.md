# JINSUL MAP v3.5.3 검증

기록일: 2026-09-07

**최종 결과: GitHub Pages 배포 및 서울 세 API 실제 수신 확인.** Worker 최종 버전 `37bade34` (서비스 1.0.2). 아래 HTTP 504 기록은 수정 전 진단 이력이다.

- 왕십리역: 2026-09-07 13:15 기준 실시간 추정 18,000~20,000명.
- 행당시장상점가: 2026년 1분기 유동인구 855,133명, 2026년 2분기 점포 323곳, 개업/폐업 10/13곳.
- 이 수치는 선택된 지정지점·공식상권 단위이며 후보지 반경 인구 합산이 아니다.
- 최종 로컬/모의 검증: 기존 회귀 34 + 지역 10 + Worker 12 = 56개 통과.

## 변경
- 검색 후보지 법정동 코드의 시도 식별자를 우선 사용하고, 주소·시도명으로 보완한다. 지역을 확인할 수 없으면 서울로 추정하지 않는다.
- 서울 실시간 인구와 공식상권의 브라우저 직접 호출을 서울 전용 HTTPS Worker POST 요청으로 변경했다.
- 전국 SGIS 기준연도 인구와 반경 상가 조회는 유지했다. 전국 실시간 유동인구를 새로 확보한 것은 아니다.
- 보고서에 검색 지역과 인구 데이터 공간 기준을 표시하고, 서울 밖 제공 대상 아님·반경 내 대상 없음·조회 불가·자료 없음·일부 연결을 구분했다.
- 점포 응답이 비어 있으면 실제 0곳으로 표시하지 않는다.
- 업로드, 통계 계산, 지도·로드뷰, 마커, 기존 필터와 외부 SDK를 보존했다.

## 로컬 검증
- `node work/test_api_v353.cjs`: 34/34 통과. 인라인 JS 구문, 기존 212개 정적 ID 및 기존 함수 보존, 모의 API 응답과 오류 처리 등.
- `node work/test_regions_v353.cjs`: 10/10 통과. 전국 시도 코드, 서울·부산·인천, 미상 지역, 반경·자료 상태 구분.
- `node work/test_seoul_worker.mjs`: 12/12 통과. CORS preflight, 허용 출처, 서비스·분기·페이지 검증, 상위 API 오류·빈 자료 코드·인증값 제거 및 Workers 지원 리디렉션 옵션.
- 왕십리본정형외과의원 1km: 내장 지리 데이터로 왕십리역 지정지점 약 401m 및 상권 중심점 선택 확인. 실시간 서버 응답 검증과는 다르다.

## 운영 상태와 한계
- 서울 전용 Worker 생성 및 코드 배포 완료. Cloudflare 편집기에서 `3bba09a9 (Active) Latest`, 구문 진단 `No Problems` 확인.
- 주소: `https://jinsul-seoul-proxy.yms0127.workers.dev/seoul`.
- R-ONE과 별도 Worker이며 Git 저장소 자동 연결을 추가하지 않았다.
- 공개 health 주소 직접 접속은 브라우저 도구에서 차단되었으나, 이후 배포된 앱과 Cloudflare HTTP 시험 화면에서는 Worker 응답을 확인했다. 서울 원본 연결 실패로 HTTP 504를 반환했다. 응답에 CORS 허용 헤더가 있으며 앱이 HTTP 상태를 읽었다. 서울 데이터 수신은 아직 실패한다.
- 현재 서울 분기 선택과 내장 매출 기준 시점은 기존 버전을 유지한다. 미제공 분기의 자료를 다른 시점 값으로 채우지 않는다.
- 기존 클라이언트 서울 키를 HTTPS POST 본문으로 중계한다. Worker 소스에 키를 넣지 않으며 상위 응답의 키 반사를 제거한다. 서울시 원본 요청은 기존 공식 HTTP:8088 경로를 사용하므로 전체 구간 HTTPS라고 설명하지 않는다.
- GitHub 업로드 및 Pages 배포 상태는 HANDOFF.md에 기록한다.

## 실제 브라우저 검증 (추가)
- GitHub Pages v3.5.3 로드 및 지도·상권 스캔 실행 확인.
- 왕십리본정형외과 1km: 지역 서울 판별, SGIS 2024 인구 25,872명, 반경 상가 4,289곳 표시. 상가 업종 표본 3,000곳이라는 기존 제한 안내 유지.
- 동일 스캔: 건축물·실거래 및 R-ONE 서울 임대료 52.8천원/㎡·공실률 6.4%·투자수익률 1.70% 표시. 심평원 신경외과는 시간 초과로 일부 실패했으며 다른 진료과 결과는 표시됐다.
- 부산 해운대구 중동 1km: 지역 부산 판별, SGIS 2024 인구 14,912명, 반경 상가 1,261곳, R-ONE 부산>해운대 표시. 서울 두 항목은 `제공 지역 아님` 확인. 동 단위 검색으로 지번이 없어 건축물 조회 생략 안내 확인.
- 위 값은 시험 당시 화면에서 관찰한 값이며, 최신 전국 실시간 유동인구를 뜻하지 않는다. 이후 Worker 수정으로 서울 수신도 확인했다 (문서 상단 최종 결과 참조).
- Cloudflare 호환성 날짜 2026-09-07 확인. 공식 문서상 비표준 포트 지원은 2024-09-02부터 기본이므로 오래된 호환성 날짜로 단정할 수 없다: https://developers.cloudflare.com/workers/configuration/compatibility-flags/#allow-specifying-a-custom-port-when-making-a-subrequest-with-the-fetch-api
- 추가 진단: Worker `2f8b822a`에서 `Invalid redirect value` 확인. Workers가 `redirect: 'error'`를 지원하지 않아 중계 코드가 원본 호출 전에 실패했다. 소스를 `manual`로 수정하고 3xx는 기존 HTTP 실패 처리로 차단한다. Worker 모의 검증에 옵션 검사 및 오류의 인증값 제거 검사를 추가했다 (12/12).
